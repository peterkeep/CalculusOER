<p><em>Instructions:</em> The function <m>y=\sin(x)</m> is plotted below, as well as a line tangent to the function at a point. Pause the animation to drag the point along the graph of the sine function and examine the tangent line at the point. Alternatively, input the <m>x</m>-value you'd like to use to construct the tangent line.</p>

<updateValue type="boolean" target="$aAnimate.animationOn" newValue="not $aAnimate.animationOn">
  <label>
    <conditionalContent>
      <case condition="$aAnimate.animationOn = true">
        Pause Animation
      </case>
      <case condition="$aAnimate.animationOn = false">
        Start Animation
      </case>
    </conditionalContent>
  </label>
</updateValue> 

<setup>
  <number name="a">0</number>
  <function name="f">sin(x)</function>
  <derivative name="fPrime">$f</derivative>
  <animateFromSequence name="aAnimate" target="$a" from="-2*pi" to="2*pi" step="0.03" animationInterval="15" animationMode="increase" animationOn="true" />
</setup>
<graph xTickScaleFactor="pi" xmin="-6.7" xmax="6.7" ymin="-3" ymax="3" identicalAxisScales displayYAxisTickLabels="false" fixAxes>
    $f
    <function name="g" styleNumber="2">$$fPrime($a)*(x-$a) + $$f($a)</function>
    <point name="afa" styleNumber="1">($a,$$f($a))
    </point>
</graph>

<conditionalContent>
  <case condition="$aAnimate.animationOn = false">
    <p>While the animation is paused, feel free to move the tangent line manually by dragging the point or by inputting specific <m>x</m>-values below.</p>
    <p><m>x=</m><mathInput name="t" prefill="0" bindValueTo="$a"/></p>
  </case>
</conditionalContent>